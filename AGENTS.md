<a id="agent-operating-guide"></a>
# 에이전트 운영 가이드

<a id="purpose"></a>
## 목적

이 저장소는 이준열의 이식 가능한 에이전트 스택입니다. 재사용 가능한 스킬, 자동화 레시피, 에이전트 대상, 도구 프로바이더 선택 방식을 보여주는 공개 포트폴리오이자 실제로 동작하는 설치 도구입니다.

<a id="boundaries"></a>
## 경계

- 핵심 구조가 특정 회사, 티켓 프리픽스(prefix), 저장소, 내부 URL, 비공개 정책에 종속되지 않게 합니다.
- 환경별 선택 사항은 생성된 프로필이나 프로바이더 어댑터에 두고, 이식 가능한 스킬에는 넣지 않습니다.
- 자격 증명, 복사한 내부 문서, 운영 데이터, 개인 식별자, 비공개 API 세부 정보를 커밋하지 않습니다.
- 일반화한 경험은 패턴이나 사례 연구로 설명합니다. 현재 증거가 없다면 공개에 맞게 정제한 구성 요소가 운영 환경에 배포되었다고 주장하지 않습니다.
- 설치 내용은 항상 미리 볼 수 있어야 합니다. 새 파일을 쓰려면 명시적인 설치 확인이 필요하고, 기존 파일을 교체하려면 명시적인 `--force` 옵션이 필요합니다.
- 런타임 어댑터를 구현하고 검증하기 전까지 자동화 항목은 이식 가능한 레시피일 뿐입니다. 카탈로그에 존재한다는 이유만으로 예약되었거나 실행 중인 자동화라고 설명하지 않습니다.

<a id="repository-contracts"></a>
## 저장소 계약

- 스킬은 Agent Skills 폴더 규약을 따릅니다. `SKILL.md` 하나와 해당 스킬에 실제로 필요한 리소스만 포함합니다.
- 각 스킬과 자동화에는 설치 도구와 포트폴리오 카탈로그가 사용하는 기계 판독형 `manifest.json`이 있습니다.
- 에이전트 설치 위치와 프로바이더 선택지는 CLI 곳곳에 분산된 조건문이 아니라 `catalog/`의 데이터로 관리합니다.
- 생성된 상태는 선택한 대상의 `.my-agent-stack/` 아래에 저장하며 Git으로 추적하지 않습니다.
- CLI는 런타임에 네트워크 서비스에 의존하지 않아야 합니다.

<a id="verification"></a>
## 검증

코드, 매니페스트, 설치 경로, 구성 요소 지침을 변경한 뒤에는 `npm run verify`를 실행합니다. 설치 도구를 변경했다면 dry-run도 검토하고 임시 디렉터리에 설치해 봅니다. 실제 사용자 구성을 덮어쓰는 방식으로 테스트하지 않습니다.

계약이 변경되면 `docs/architecture.md`, 관련 ADR, `docs/verification.md`를 함께 갱신합니다.
